<%= form_with(model: custom_fieldset, local: true) do |form| %>
  <% if custom_fieldset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(custom_fieldset.errors.count, "error") %> prohibited this custom_fieldset from being saved:</h2>

      <ul>
        <% custom_fieldset.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
