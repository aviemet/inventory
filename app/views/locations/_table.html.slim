table.table-auto
  thead
    tr
      th Name
      th Parent
      th.table-column-fit.text-center Actions
  tbody
    - locations.each do |location|
      tr
        td= link_to location.name, location_path(location)
        td= link_to location.parent.name, location_path(location.parent) if location&.parent_id
        td nowrap="nowrap" 
          = render "shared/buttons/edit", path: edit_location_path(location)
          = render "shared/buttons/delete", path: location_path(location)
