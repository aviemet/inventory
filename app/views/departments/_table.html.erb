<table class="table-auto">
  <thead>
    <tr>
      <th>Name</th>
      <th>Location</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% departments.each do |department| %>
      <tr>
        <td><%= link_to department.name, department_path(department) %></td>
        <td><%= link_to department.location.name, location_path(department.location) if department.location %></td>
        <td><%= link_to 'Edit', edit_department_path(department) %></td>
        <td><%= link_to 'Destroy', department_path(department), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>