= form_with(model: nic, local: true) do |form|
  - if nic.errors.any?
    #error_explanation
      h2
        = pluralize(nic.errors.count, "error")
        | prohibited this nic from being saved:
      ul
        - nic.errors.full_messages.each do |message|
          li= message
  .field
    = form.label :mac
    = form.text_field :mac
  .field
    = form.label :item_id
    = form.text_field :item_id
  .actions
    = form.submit
