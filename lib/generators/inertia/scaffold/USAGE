Description:
    Generates react views for inertiajs according to folder structure set in initializer, and structure of record if provided

    scaffold: Generates model, migration, controller, views and resource routes
    controller: Generates controller and views
        If MODEL_NAME matches an existing record, controller passes that data to views
    views: Generates only views
        Generates resource blank views

Example:
    bin/rails generate inertia:scaffold MODEL_NAME
    bin/rails generate inertia:controller NAME
    bin/rails generate inertia:views NAME
