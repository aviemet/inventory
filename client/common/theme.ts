import { Theme } from "react-native-elements";

const theme : Theme = {
	// Theme defaults
	colors: {
		primary: 'orange',
		secondary: 'blue',
		// grey0;
		// grey1;
		// grey2;
		// grey3;
		// grey4;
		// grey5;
		// greyOutline;
		// searchBg;
		// success;
		// error;
		// warning;
		// divider;
		platform: {
			ios: {
				// primary;
				// secondary;
				// success;
				// error;
				// warning;
			},
			android: {
				// Same as ios
			},
		},
	},

	// Custom styles
	Button: {
		raised: true
	},
};

export default theme;